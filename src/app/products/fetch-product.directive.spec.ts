import { FetchProductDirective } from './fetch-product.directive';

describe('FetchProductDirective', () => {
  it('should create an instance', () => {
    const directive = new FetchProductDirective();
    expect(directive).toBeTruthy();
  });
});
